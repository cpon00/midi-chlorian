cred x = 5
emit(1 - 2 - 3 - 4 + x)
emit(0 * 0)

emit(1+0)
emit(1-0)
emit(5/1)
emit(5*1)
emit(5*1*0)
emit(4 * 1)
emit(4+3+2 ** 1)
emit(light and light)
emit(darth light)
holocron<cred, transmission> carterSmellz = <3: "nice", 2: "nice">
